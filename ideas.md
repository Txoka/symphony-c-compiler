whole call evaluation should also work on call towers
when a calls b and b calls c etc, that should be able to get evaled
and that also removes calls so opens inlining possibilities for functions so it should happen before


ok, the prioritized elements for the next branch should be those that will help the most close the gap vs gcc on bigprime, pi, insertion sort and sensor report, lets assume we dont care about BSS filling overhead of our programs, thats a good trade that can be removed by adding zeros or assuming zeroed... Document very well that thats what should be done on the next branch, how that branch should be called, you investigate what optimizations those should be by checking the machine codes for those programs and generate the report conclussions and prioritized optimizations, they may be the ones we already looked into, but worth double-checking after the optimizations we already introduced.